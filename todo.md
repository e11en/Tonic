# todo.md — active phase

> See `ROADMAP.md` for all phases. This file holds only the **current** phase, written out as
> concrete steps. When the phase is done, empty this file and refill with the next phase.

## No active phase

**Tonic is feature-complete — all roadmap phases (0–9) are shipped and verified.**

The full DAW is in place: multi-track timeline + mixer, IndexedDB persistence, sample upload &
playback, microphone recording, instrument tracks + piano roll, drum machine, per-track effects
rack, Web MIDI input, and beginner onboarding — all drivable by both the UI and the MCP live
bridge through one shared action layer.

Possible future work (not on the original roadmap): custom drum samples per lane, clip resize/trim,
undo/redo, project export (render to wav), sampler instruments, quantize, automation lanes.
