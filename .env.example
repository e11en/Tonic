# Tonic environment (placeholders — copy to .env.local and adjust)

# WebSocket bridge port the browser connects to (used from Phase 1 onwards).
# The MCP server hosts the bridge on this port; the browser app connects to it.
VITE_TONIC_WS_PORT=8765
